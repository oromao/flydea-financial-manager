{
  "framework": "nextjs",
  "buildCommand": "[ -z \"$DATABASE_URL\" ] && export DATABASE_URL='file:./prisma/prod.db' || true; npx prisma generate && next build",
  "installCommand": "npm install",
  "regions": ["gru1"],
  "crons": [
    {
      "path": "/api/cron/recurrence",
      "schedule": "0 6 * * *"
    }
  ]
}
