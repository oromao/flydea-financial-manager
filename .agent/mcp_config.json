{
  "mcpServers": {
    "vercel": {
      "description": "Vercel deployments, logs, env vars, domains, projects — 50+ tools",
      "command": "npx",
      "args": ["-y", "@robinsonai/vercel-mcp"],
      "env": {
        "VERCEL_API_TOKEN": "YOUR_VERCEL_TOKEN"
      }
    },
    "shadcn": {
      "description": "Adiciona, lista e gerencia componentes shadcn/ui via IA",
      "command": "npx",
      "args": ["-y", "shadcn@4.6.0", "mcp"]
    },
    "playwright": {
      "description": "Navegação, screenshots, testes visuais e interação com páginas web via Playwright",
      "command": "npx",
      "args": ["-y", "@playwright/mcp@latest"]
    },
    "github": {
      "description": "Gerenciamento de PRs, issues, branches, releases, code review",
      "command": "npx",
      "args": ["-y", "@fre4x/github"],
      "env": {
        "GITHUB_TOKEN": "YOUR_GITHUB_TOKEN"
      }
    }
  }
}
